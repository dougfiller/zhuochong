# Windows AI 桌宠微信回复助手首个可运行产品需求基线

> 文档状态：当前实施基线
> 版本：v0.1
> 日期：2026-08-04
> 目标平台：Windows 11 x64
> 微信客户端：开发和验收时指定的 Windows 微信 4.0.x 小版本
> 核心原则：优先直接复用参考代码；没有现成代码的部分只实现最少胶水；先交付真实可运行闭环，不建设平台化架构。

## 1. 产品定义

本版本是在 Work Review 桌面应用基础上增加的微信回复建议功能，不开发另一套临时程序。

用户在一个当前处于前台的微信单聊窗口中主动触发后，产品截取并裁剪当前聊天区域，使用本地 OCR 读取当前可见文字，通过 Work Review 已有模型配置生成一条建议回复，并在现有桌宠气泡中显示。用户点击复制后，自行在微信中粘贴、修改和发送。

本版本只解决以下闭环：

```text
当前前台微信单聊
→ 用户主动触发
→ 截图并裁剪固定聊天区
→ 本地 OCR
→ 生成一条建议回复
→ 桌宠气泡显示
→ 用户点击复制并自行发送
```

完成该闭环即完成 v0.1，不再以旧版完整 v1.0 范围作为当前交付条件。

## 2. 支持范围与固定假设

- 只支持 Windows 11 x64。
- 只支持开发和验收时实际使用的一个 Windows 微信 4.0.x 小版本。
- 只支持当前处于前台的一个微信单聊窗口。
- 微信窗口必须完整放在主显示器上；不支持跨屏或副屏。
- 只支持一种经过实机验证的微信主题、DPI、窗口尺寸和聊天布局。
- 聊天区使用固定坐标或固定比例裁剪；允许直接写在代码或一个简单配置文件中。
- 最终只固定使用一个本地 OCR：优先使用 Work Review 现有 Windows OCR；若在目标机器确认不可用，允许一次性改用 `参考/Umi-OCR-main` 的 RapidOCR，二者不并存切换。
- 只使用 Work Review 当前已有的模型配置和模型调用能力。
- 同一时间只处理一个请求。
- 不要求兼容群聊、多微信实例、多主题、多 DPI、多屏、任意窗口尺寸或其他微信版本。

## 3. 功能需求

### FR-01 手动触发

- 只有用户明确触发时，产品才执行一次微信回复建议流程。
- 触发入口只实现一种：现有桌宠中的按钮、托盘入口或固定快捷键，选择最先能稳定工作的方式即可。
- 如果点击入口会使微信失去前台状态，可以显示短暂倒计时，让用户切回微信后再采集。
- 真正截图时，当前前台窗口必须是支持范围内的微信窗口。
- 当前前台窗口不是微信时，显示简单错误并结束本次处理。
- 前台应用判断直接使用 Work Review 已归一化的 `app_name == "WeChat"`，不另建进程识别规则。
- 本版本不保存长期窗口绑定，程序重启后不恢复绑定状态。
- 本版本不在后台自动截图、自动监听或自动调用模型。

### FR-02 截图与固定区域裁剪

- 复用 Work Review 已有的前台窗口信息、窗口矩形和截图代码。
- 每次触发只截取一次当前画面。
- 截图前临时隐藏产品自己的桌宠窗口和气泡；截图调用无论成功或失败都立即恢复。
- 直接读取 `ScreenshotResult.ocr_source_path` 对应的原始 PNG，不使用可能已经缩放的归档 JPEG。
- v0.1 只处理位于主显示器上的微信窗口；先按前台窗口矩形裁出微信窗口，再按相对窗口的 `[left, top, right, bottom]` 比例裁出聊天区。
- 只有裁剪后的聊天区图片进入 OCR 和本版本的数据保存目录。
- 裁剪成功后删除本次流程产生且不再需要的整屏归档图和 OCR 临时源图。
- 不建设微信窗口选择列表、精确 HWND 身份数据库、自动校准框、视觉锚点或布局自适应。
- 截图过程不得向微信输入文字、点击界面、滚动记录或改变聊天对象。

### FR-03 本地 OCR

- 裁剪后的聊天区图片必须在本机完成 OCR。
- 不使用远程 OCR，也不把聊天区图片发送给 OCR 服务商。
- 首选直接调用 Work Review 已有 Windows OCR；若目标机器实测不可用，可一次性改为直接调用 Umi-OCR 的 RapidOCR，此后只保留选定的一条路径。
- 只要求输出当前可见聊天区的纯文本；现有 OCR 返回的文字框可保留，但不为本版本另建统一 OCR 契约。
- 不要求识别消息发送方向、具体发送人、时间标签、群聊成员或消息新旧。
- OCR 无文字或失败时，显示失败信息，并且不调用回复模型。
- 不建设 OCR 后端切换、候选对比、语言选择或 `path/bytes/base64` 多输入体系。

### FR-04 回复建议

- 复用 Work Review 已有 LLM 配置和模型请求代码。
- 使用一个固定提示词，把本次 OCR 得到的当前可见聊天全文交给模型。
- 模型只返回一条简短、自然、可直接复制的中文建议回复。
- 不建立跨触发会话记忆，不判断新消息，不区分我方与对方消息。
- 请求进行期间禁用再次触发，完成或失败后恢复按钮即可。
- 模型失败、超时或返回空内容时，在气泡中显示简单失败信息。
- 不实现请求取消、五元组校验、自动重试、请求版本体系或专用错误码体系。

建议使用的固定提示词：

```text
你是微信回复助手。根据以下当前可见微信单聊 OCR 文本，生成一条简短、自然、可以直接发送的中文回复。
只输出回复正文，不解释。

{ocr_text}
```

### FR-05 桌宠气泡与复制

- 复用 Work Review 现有桌宠窗口和气泡。
- 首次运行验收前直接启用 Work Review 的桌宠开关。
- 气泡只需要显示等待、生成中、成功文本或失败文本。
- 建议结果使用持续气泡，直到用户复制或主动关闭，不沿用约 4.2 秒后自动消失的普通气泡。
- 成功时提供一个“复制”按钮。
- 用户点击复制后，剪贴板内容必须等于当前建议正文。
- 未点击复制时，产品不得自动改写剪贴板。
- 产品不得自动聚焦微信、自动粘贴、自动输入或自动发送。
- 最终粘贴、修改、确认和发送全部由用户完成。
- 本版本不提供 TTS。

### FR-06 最简本地落盘

- 开发调试阶段允许先不落盘；首个交付版本保存裁剪图和一份 JSON Lines 记录。
- 建议保存位置：

```text
%LOCALAPPDATA%/Zhuochong/data/wechat/YYYY-MM-DD/HHMMSS.png
%LOCALAPPDATA%/Zhuochong/data/wechat/events.jsonl
```

- `events.jsonl` 每条记录最多包含：

```text
触发时间
微信窗口标题
截图相对路径
OCR 全文
建议正文
处理结果
```

- 不新增微信专用 SQLite 表。
- 不实现内容寻址、引用计数、数据库事务编排、存储配额、删除审计和孤儿文件检查。
- 程序不主动清理本目录；本版本不实现五年到期自动删除。
- 用户需要删除时，可以直接删除本目录中的文件。
- 本版本不上传微信截图、OCR 或建议记录。

### FR-07 Work Review 原有能力处理原则

- Work Review 的工作记录、应用分类、时间线、日报、普通截图、远程上传、设置页和现有桌宠能力原样保留。
- 本次开发不重写、不整理、不统一这些已有模块。
- 这些能力不纳入 v0.1 的新增功能验收，也不要求补齐新的权限矩阵。
- 只要求派生应用仍能启动，并且没有为了新增微信功能而故意删除原有入口。

## 4. 必须保留的微信交互边界

微信回复功能不得：

- 注入微信进程、DLL 或插件。
- 读取微信协议、本地聊天数据库、进程内存或内部对象。
- 使用 UI Automation 读取或操作微信控件。
- 自动滚动聊天记录、切换联系人、输入文字、粘贴或发送。
- 在用户未点击复制时改写剪贴板。
- 在没有用户手动触发时自动截图或调用模型。

这些限制只约束新增的微信回复链路。Work Review 原有工作记录若按其原设置执行后台普通截图，不属于微信回复链路，也不纳入本版本验收。

这些是产品行为边界，不要求为此建设权限中心、策略引擎或审计系统。

本版本不新增静态加密、Credential Manager/DPAPI、IPC 鉴权、随机令牌、日志脱敏、安全扫描或数据域隔离要求；直接沿用 Work Review 当前行为即可。

## 5. 单一配置

新增微信功能最多使用一个简单配置，也可以先直接写成代码常量：

```json
{
  "wechat_app_name": "WeChat",
  "chat_roi": [0.25, 0.10, 0.98, 0.78],
  "reply_prompt": "根据以下当前可见微信单聊内容，给出一条自然简短的建议回复："
}
```

- 不要求新增专门的微信设置页。
- LLM 地址、密钥和模型继续使用 Work Review 原有配置。
- 不建设多环境配置、配置迁移或 schema 版本。
- ROI 不适配当前机器时，直接修改四个数值，不先开发可视化校准工具。

## 6. 明确后置的功能

以下功能不属于 v0.1 开发或验收范围：

- 群聊和群聊发送人识别。
- 自动监听、定时截图和自动触发。
- 新消息判断、重复抑制和历史滚动识别。
- 我方/对方消息方向识别。
- 聊天对象隔离、会话键、切换恢复和窗口绑定恢复。
- 多微信实例、多主题、多 DPI、多屏和任意窗口尺寸。
- 精确 HWND WGC 捕获及完整兼容矩阵。
- 13 项独立授权、授权依赖矩阵和数据域隔离。
- 五年自动到期、容量预警和自动清理。
- 微信记录远程上传。
- Live2D、角色切换、动作和表情。
- ASR、TTS、摄像头、屏幕感知、整屏工作分析和主动建议。
- 直播、MCP 和通用电脑控制。
- 使用多模态 LLM 替换本地 OCR。
- Python sidecar、IPC schema、跨语言类型生成和通用适配器。
- 安装签名、自动升级、回滚、SBOM 和全量自动化测试。

后续功能只有在 v0.1 实际可用后，根据真实问题逐项决定，不提前为它们建设接口或占位模块。

## 7. 最小验收场景

### AC-01 应用启动

- Work Review 派生应用能够在目标 Windows 11 机器启动。
- 启用现有桌宠后，桌宠窗口能够显示。
- 在 Work Review 原有设置中配置并测试一个实际可用的模型。

### AC-02 手动回复完整闭环

1. 用户打开一个支持布局的微信单聊并使其处于前台。
2. 用户明确触发一次“生成回复”。
3. 产品取得当前画面并裁剪固定聊天区。
4. 本地 OCR 输出当前可见聊天文字。
5. Work Review 现有模型能力根据 OCR 文字生成一条建议。
6. 建议显示在桌宠气泡中。
7. 用户点击复制，剪贴板内容与气泡建议正文一致。
8. 用户自行在微信中粘贴、修改和发送。

### AC-03 错误反馈

- 前台不是微信、截图失败、OCR 无文字或模型失败时，显示一条可理解的失败信息。
- 失败时不得出现可复制的伪建议。
- 失败后用户能够再次手动触发；不要求自动重试或恢复。

### AC-04 本地 OCR

- 关闭网络后，截图裁剪和 OCR 阶段仍能完成。
- 远程 LLM 因断网失败可以直接显示失败，不影响本地 OCR 验收。

### AC-05 最简落盘

- 首个交付版本能够找到本次裁剪图。
- `events.jsonl` 中能够找到对应 OCR 文字、建议正文和处理结果。
- 不验收自动清理、五年到期、数据库查询或远程上传。

### AC-06 微信操作边界

- 全流程没有自动输入、粘贴、点击发送、切换联系人或滚动聊天记录。
- 只有用户点击复制后，剪贴板才发生变化。

## 8. 完成定义

满足 AC-01～AC-06，即认定首个可运行产品 v0.1 完成。

Work Review 原有工作助手功能、群聊、自动监听、完整权限体系、长期存储和多模态桌宠功能均不阻塞本版本完成。它们进入后续需求池，待当前闭环实际可用后再逐项决定是否开发。

本文件自 2026-08-04 起替代旧版完整 v1.0 范围，作为当前开发和验收的唯一需求基线。
