# Windows AI 桌面宠物助手最终需求文档

> 文档状态：需求基线｜版本：v1.0｜日期：2026-08-02｜目标平台：Windows 11｜微信客户端：Windows 微信 4.0.x 系列
> 本文档定义产品需求、版本范围、权限边界和验收口径。初版的微信及工作记录文字识别固定采用本地 OCR 能力，后续版本规划改用多模态 LLM 替代这两条 OCR 识别链路；除此之外，不锁定编程语言、桌面框架、数据库或具体实现方案。

## 1. 产品定义

本产品是一个运行在 Windows 11 上的 Live2D AI 桌面宠物助手，以桌宠形象作为统一交互入口，同时提供两类能力：

1. **微信 AI 回复提词器**：只通过截图和 OCR 识别当前处于前台的已选定微信聊天窗口，生成建议回复，通过桌宠气泡和可选语音展示；只允许用户将建议复制到系统剪贴板，粘贴和发送必须由用户本人完成。
2. **个人工作记录与通用桌面助手**：在用户知情和分别授权后，记录应用使用、工作时长、屏幕截图与 OCR，形成分类统计、时间线、日报、长期整屏归档和远程上传，并通过语音、摄像头和主动建议辅助用户完成工作。键鼠活动状态无需单独授权，由系统直接被动监测，仅用于活跃/空闲判定、工作节奏统计和桌宠状态反馈。直播、MCP 工具和通用电脑控制属于后续版本能力，不纳入初版交付。

产品仅面向设备所有者的个人使用，不定位为员工监控、隐形追踪、考勤或绩效评估工具。

### 1.1 初版支持环境

- 操作系统限定为 Windows 11。
- 微信客户端限定为 Windows 微信 4.0.x 系列，不包含企业微信。
- 微信提词器只处理由用户主动选择并绑定的单个微信顶层聊天窗口。
- 多微信实例中的其他窗口、登录页、设置页、小程序窗口以及非聊天窗口均不得自动进入微信提词器采集和回复链路。
- 上述已选定窗口限制只适用于微信提词器；工作记录属于独立权限域，在获得工作记录、工作记录截图和工作记录 OCR 授权后，可以按工作记录规则采集前台应用或整屏中的微信画面与 OCR。
- 微信客户端版本不属于已验证的 4.0.x 系列，或 4.0.x 界面布局无法可靠识别时，系统必须停止微信采集并显示不受支持状态，不得继续猜测聊天区域。

## 2. 产品目标

- 在本文档明确的初版范围内尽快完成可用原型，但不预先锁定 Python、Rust 或其他语言。
- 在不读取微信内部数据、不注入、不自动发送的前提下，提供有上下文的回复建议。
- 以桌面宠物作为低干扰、长期常驻的信息和交互载体。
- 持续记录和整理个人工作上下文，支持回看、统计、日报、归档以及基于当前或近期数据的整屏工作分析。
- 初版通过可配置的 LLM、ASR、TTS、摄像头和屏幕输入形成通用桌面助手；后续版本再扩展直播、MCP 和通用电脑控制。
- 用户始终可以知道什么正在被采集、什么正在被上传，并可随时暂停、关闭或删除数据。

### 2.1 版本范围

初版必须完成以下能力，均属于 v1.0 的交付和验收范围：

- 当前处于前台的已选定微信聊天窗口识别、截图、本地 OCR、消息整理、增量判断、回复建议、气泡展示、可选 TTS、复制和手动发送链路。
- 个人工作记录、工作时长、应用分类、时间线、日报、长期整屏截图与 OCR 归档、远程上传、键鼠活动监控和隐私数据控制。
- Live2D 桌宠、角色动作与表情、气泡、ASR、TTS、摄像头与屏幕感知、整屏工作分析、主动建议以及角色和模型配置。
- 本文档中除明确标记为“后续版本”的其他全部功能需求和对应验收场景。

以下能力不纳入初版，属于后续版本：

- 直播模式。
- MCP 工具。
- 通用电脑控制。
- 使用多模态 LLM 图像识别替代初版微信及工作记录链路中的本地 OCR。

后续版本需求保留在本文档中用于约束产品方向，但不得作为初版延期或验收不通过的理由。

本文档中涉及直播、MCP、通用电脑控制和多模态 LLM 替换本地 OCR 的安全、状态、授权、配置及验收条款，只在对应后续能力实际交付时生效；初版不需要实现这些后续能力的占位入口或执行模块。

## 3. 强制权限边界

### 3.1 微信保护域

“微信保护域”的规则优先级高于语音命令、主动助手、MCP 工具和通用电脑控制。

“已选定微信聊天窗口”是指用户开启微信提词器时主动选择并绑定的单个 Windows 微信 4.0.x 顶层聊天窗口。只有该窗口当前处于前台、绑定仍然有效且存在有效聊天区域时，系统才可以进入微信提词器采集和回复链路。窗口关闭、重新创建或身份无法确认时，原绑定立即失效，必须暂停微信提词器采集并由用户重新选择。该限制不替代工作记录功能自身的独立授权和采集规则。

允许的行为：

- 使用 Windows 系统提供的只读窗口信息判断当前前台窗口是否为已选定微信聊天窗口。
- 取得当前处于前台的已选定微信聊天窗口的标题、进程标识、坐标和尺寸。
- 在用户分别授权并开启“微信截图”和“微信 OCR”后，对当前处于前台的已选定微信聊天窗口进行截图，对截图中的聊天内容进行 OCR，截图与 OCR 结果按 FR-WX-07 落盘保存。
- 将整理后的聊天上下文交给大模型生成建议。
- 用桌宠气泡显示或用 TTS 朗读建议。
- 在用户明确点击“复制”后，将建议写入系统剪贴板。
- 允许时间线、日报和工作分析在分别获得对应授权后，读取已按 FR-WX-07 保存的微信截图与 OCR 数据。
- 在用户另外开启远程上传后，上传已获授权的微信截图、OCR 结果及其关联记录。

禁止的行为：

- 向微信进程注入代码、DLL 或插件。
- 微信提词器读取微信协议、本地聊天数据库、进程内存或内部对象。
- 使用辅助功能、UI Automation 或其他手段读写微信控件。
- 模拟鼠标、键盘、自动滚动聊天记录、自动切换联系人、自动粘贴、按回车或点击发送。
- 在用户未点击“复制”时自动改写剪贴板。
- 由任何模型、语音命令、主动任务或 MCP 工具绕过上述窗口级限制。

即使用户说出“帮我发送”，或模型/MCP 产生了操作微信的工具调用，系统也必须停在“生成建议”或“等待用户复制”阶段。

### 3.2 通用助手权限域

- 通用电脑控制只能在微信之外的目标窗口中运行。
- 一旦操作目标、输入焦点或动作落点属于微信，必须在产生输入事件前阻断。
- 麦克风、摄像头、屏幕感知、整屏工作分析、直播连接、MCP 和电脑控制均需要明确的开关和状态提示。
- 不可逆、高风险或可能对外发布信息的操作需要用户确认，并必须提供立即停止入口。
- 本需求基线只要求按目标窗口、当前前台窗口和输入焦点实施窗口级微信保护，不把间接操作识别或焦点切换竞态防护作为验收要求；其他章节中的“不得绕过”均以该窗口级覆盖范围为限。

### 3.3 默认关闭与分别授权

- 首次安装或首次启动时，下列全部授权项必须保持关闭；每个授权项必须分别提供独立开关和独立授权，开启其中一项不得自动开启其他任何一项。
- 用户关闭某项能力后，系统必须停止该能力产生的后续采集、识别、外部请求、上传或操作。

初版共 13 个授权项。每项的管辖范围以本表为唯一定义，其他章节引用授权项名称时不得扩大或缩小本表定义：

| 授权项 | 管辖范围 |
| --- | --- |
| 微信提词 | 微信回复提词器总开关：窗口绑定、回复建议流水线和气泡展示 |
| 微信截图 | 已选定微信聊天窗口的截图采集与落盘（五年固定保留） |
| 微信 OCR | 微信截图的本地 OCR 处理与结果落盘（五年固定保留） |
| 工作记录 | 前台应用、窗口标题、活动时间和分类的采集与落盘 |
| 工作记录截图 | 工作记录链路内全部截图的采集与落盘：活动记录截图和长期整屏归档，含归档画面中出现的微信内容，按工作记录保留规则执行 |
| 工作记录 OCR | 工作记录链路截图的本地 OCR 处理与结果落盘 |
| 屏幕感知 | 助手实时理解屏幕录制或截图内容（FR-PET-06），只形成临时上下文，不长期落盘 |
| 整屏工作分析 | 基于当前/近期屏幕内容与工作记录数据生成工作分析（FR-PET-07） |
| 主动建议 | 主动建议的生成、展示与朗读 |
| 麦克风 | 语音采集与 ASR |
| 摄像头 | 摄像头画面采集与理解 |
| 云模型 | 远程 LLM、远程 ASR/TTS、远程多模态等云服务调用 |
| 远程上传 | 本地记录向远程存储的上传 |

- 长期整屏归档不是独立授权项，而是“工作记录截图”和“工作记录 OCR”管辖下的采集行为；其截图周期、保留期限和存储上限属于配置项（FR-WR-05）。
- 键鼠活动监控不是授权项：系统无需授权即可直接被动监测按键、鼠标移动和鼠标点击的活动状态，查询系统最后输入时间等只读系统调用也不视为需要授权的监控行为；该信号只记录活动状态、不记录按键内容，具体规则以 FR-WR-07 为准。
- 直播、MCP、通用电脑控制和多模态 LLM 图像识别不属于初版授权项，但在后续版本提供时也必须默认关闭并分别授权。

## 4. 功能需求

### 4.1 微信 AI 回复提词器

#### FR-WX-01 当前处于前台的已选定微信聊天窗口识别

- 系统必须能识别 Windows 当前前台窗口的应用身份、窗口标题和窗口边界。
- 用户必须能够从有效的 Windows 微信 4.0.x 顶层聊天窗口中主动选择并绑定一个窗口。
- “微信提词”是微信回复提词器的功能总开关，默认关闭并需分别授权，控制窗口绑定、回复建议流水线和气泡展示；关闭“微信提词”必须停止微信截图、OCR 和回复模型调用。
- 运行依赖链为“微信提词”→“微信截图”→“微信 OCR”：只有“微信提词”已开启且存在有效窗口绑定时，“微信截图”才可以运行；开启其中任一项不得自动开启其他项。仅开启“微信提词”而“微信截图”或“微信 OCR”未开启时，提词器必须处于等待前置授权状态，不截图、不 OCR、不调用回复模型，并提示缺少的授权。
- 只有已选定微信聊天窗口当前处于前台、绑定仍然有效且存在有效可见聊天区域时，才能进入微信 OCR 回复链路。
- 多微信实例中的其他窗口、登录页、设置页、小程序窗口及其他非聊天窗口即使处于前台，也不得自动进入微信提词器采集和回复链路。
- 微信客户端版本或界面布局不符合已验证的 Windows 微信 4.0.x 支持范围时，不得采集或生成建议。
- 已选定微信聊天窗口最小化、不在前台、被关闭、被重新创建、屏幕锁定或无有效聊天区域时，不得采集或生成聊天建议。
- 已选定微信聊天窗口移动、缩放或切换显示器后，截图来源必须仍与该已选定窗口对应；无法继续确认对应关系时，绑定失效并要求用户重新选择。

#### FR-WX-02 定时截图与聊天区域

- 系统必须按可配置周期截取当前处于前台的已选定微信聊天窗口。
- 微信回复链路的 OCR 输入不得包含其他显示器或微信窗口外的桌面区域。
- 系统需要从已选定微信聊天窗口截图中确定聊天标题和聊天记录区域，排除联系人列表、输入框等无关内容。
- 截图不得改变微信焦点、窗口状态或聊天界面。

#### FR-WX-03 OCR 聊天文本与消息结构

- 系统需要对当前可见聊天记录进行 OCR，并保留文本块的内容、置信度、坐标和顺序。
- 需要将几何 OCR 结果整理成聊天消息，并在有充分依据时识别对方消息、我方消息、可见时间和当前聊天对象。
- 消息必须保持屏幕中的时序顺序；无法确定归属时不得伪造发送者或时间。
- 传给回复模型的是当前聊天的整理后上下文，不是长期整屏归档中的其他应用内容。
- 初版支持 Windows 微信 4.0.x 的单聊和群聊。群聊消息只有在能够可靠确认群聊对象、具体可见发送者以及消息属于非我方时，才可以作为新对方消息触发建议。
- 系统只能基于可见显示名称和聊天类型识别当前聊天对象，无法检测显示名称相同但实际不同的聊天对象；显示名称不代表可跨切换追踪的稳定身份，会话状态的跨切换限制按 FR-WX-04 执行。

#### FR-WX-04 增量识别与重复抑制

- 系统需要按聊天对象隔离上下文和消息内容状态，不得将不同聊天混在同一个增量链路中。
- 会话键必须至少绑定已选定窗口身份、当前聊天对象、聊天类型、本次窗口绑定周期和本次连续观察周期；窗口绑定期内每次聊天对象切换都必须开始新的连续观察周期并产生新会话键。显示名称不得单独作为稳定会话键。当前聊天对象无法确认时，会话键必须使用按本次窗口绑定隔离的“未知会话”保留值。
- 需要按识别后的消息内容或等价稳定标识进行增量判断。
- 首次绑定窗口、程序首次观察某个聊天、重新进入聊天或窗口绑定重新建立时，首次采样只能建立基线，屏幕中已经可见的历史消息不得被当作新消息触发模型。
- 只有在同一会话基线建立后新出现且能够可靠判定为对方发送的消息，才可以触发回复模型。
- 用户向上滚动后出现的历史消息、重新进入聊天后重新出现的旧消息以及同一历史内容的重新排版不得触发新建议。
- 群聊中必须按同一群聊会话隔离状态，并保留能够可靠识别的具体发送者；无法确认群聊、具体发送者或消息归属时不得触发。
- 聊天对象发生切换时（包括切换到与之前显示名称相同的联系人或群聊），必须停止触发并清空上一会话的增量状态、上下文和建议，新聊天的首次采样只建立基线；任何情况下不得跨切换复用会话状态。
- 当前聊天对象、发送方或消息新旧状态任一无法确认时，必须停止自动触发，清空或隔离相关旧状态，显示无法确认的状态，不得猜测，也不得调用回复模型。
- 聊天内容没有有效变化时，不重复调用回复模型，不重复显示相同建议。
- 只有“新出现的对方消息”可以触发回复模型；我方消息可作为上下文，但不得单独触发新建议。
- 光标闪烁、时间文字变化、表情动画等非消息变化不应触发新建议。

#### FR-WX-05 LLM 回复建议

- 系统必须将当前聊天的新对方消息和必要的可见上下文交给可配置的大模型。
- 模型输出必须被定义为“建议回复”，不得声称或暗示已经发送。
- 支持本地模型或远程 API，不锁定具体提供商、模型或协议。
- 调用远程模型前，用户需要能够知道哪些聊天文本会离开本机。
- “截图版本”表示本次请求实际使用的有效聊天内容快照版本；周期性重复截图在聊天内容和窗口状态都没有有效变化时不得创建新版本。
- 每次模型请求必须绑定已选定窗口身份、会话键、截图版本和唯一请求 ID。
- 请求进行期间，只要聊天对象切换、窗口绑定失效、截图版本被更新或出现更新的新消息，旧请求就必须被取消；无法取消时，其返回结果必须被丢弃。
- 已失效请求的结果不得显示、不得朗读、不得写入剪贴板，也不得恢复为当前建议。
- 模型超时、返回空内容或格式错误时，只显示可理解的失败状态，不得引发剪贴板或电脑操作。
- 未配置可用模型时，不得产生隐式外网请求。

#### FR-WX-06 气泡展示、复制和手动发送

- 建议回复必须在桌宠旁边的悬浮气泡中完整显示。
- 气泡需要显示生成中、成功、失败等状态，并允许用户关闭或忽略建议。
- 气泡必须提供“复制到剪贴板”按钮；只有已经生成完成且请求仍然有效的建议才能启用该按钮。
- 点击复制后，剪贴板内容必须与当前最终建议正文一致，不附带角色名、引号、状态文字或隐藏内容。
- 复制动作不得聚焦微信、不得写入微信输入框、不得触发发送。
- 最终粘贴、修改、确认和发送始终由用户手动完成。

#### FR-WX-07 微信截图与 OCR 采集、保存和读取

- “微信截图”控制截图采集和截图持久化，“微信 OCR”控制本地 OCR 处理和 OCR 结果持久化；两项能力必须各自拥有默认关闭的独立开关并分别获得用户授权。
- 微信提词器的截图、OCR 与回复建议链路以“微信截图”和“微信 OCR”均已授权并开启为前提；使用微信提词器必然产生本地落盘记录并按五年规则保留，不存在只运行提词而不产生记录的使用方式。
- 只有“微信截图”已经获授权并开启时，“微信 OCR”才可以运行；开启“微信 OCR”不得自动开启“微信截图”。
- “微信截图”开启后，系统必须保存授权范围内的微信整窗截图或聊天区域裁剪图；“微信 OCR”同时开启后，还必须保存 OCR 结果。两类记录均需关联采样时间、已选定窗口绑定标识、会话键和截图版本。
- 身份不确定或未知会话状态下，已开启的截图与 OCR 仍须落盘；会话键必须填写为按本次窗口绑定标识隔离的“未知会话”保留值，不得猜测、留空或复用任何其他会话的会话键。未知会话记录不得触发回复模型，用户可回看和删除，适用相同的五年保留规则。
- 初版不提供“只在内存中处理微信截图或 OCR 而不落盘”的模式；存储空间不足或落盘失败时，必须暂停新的微信截图、OCR、回复模型调用和上传，显示原因，已有记录仍可读取。
- 已保存的微信截图和 OCR 记录可以由用户回看，也可以在分别获得工作记录、工作分析或远程上传授权后，由对应模块读取或上传。
- 已成功保存且未被用户主动删除的微信截图与 OCR 记录，自采样时间起保留五年，到期后自动删除；用户主动删除优先于五年保留要求。
- 本功能不要求对业务截图、OCR 结果和关联记录实施静态存储加密，但仍必须遵守文件访问权限、日志脱敏、凭据保护以及外发前单独授权、过程可见且可关闭的要求。

### 4.2 个人工作记录与回顾

#### FR-WR-01 活动记录和工作时长统计

- 只有工作记录已由用户授权并开启时，系统才可以持续记录前台应用、窗口标题、活动时间和分类；工作记录截图、工作记录 OCR 还必须分别授权并开启后才能采集对应字段，未开启的字段不得采集。
- 工作时长需要按日期、应用、分类和时段统计，并支持小时级分布。
- 需要正确处理应用切换、暂停记录、系统锁屏和用户空闲，避免将时长错误归属给下一个应用。
- 用户空闲判定不依赖任何授权项：系统直接使用被动监测的键鼠活动状态信号判定活跃与空闲，达到空闲阈值后停止累计有效工作时长，具体规则见 FR-WR-07。
- 用户可以配置工作时段，区分有效工作、休息和非工作时间。

每条活动记录至少需要表达：

- 时间戳。
- 应用名和窗口标题。
- 分类和持续时间。
- 截图或截图引用（工作记录截图开启时）。
- OCR 文本（开启时）。
- 可选的远程资源地址。

#### FR-WR-02 应用和工作语义分类

- 系统必须支持内置分类、用户自定义分类和手动覆盖。
- 常见开发、办公、浏览器、沟通、会议、娱乐等应用需要获得稳定分类。
- 未识别活动必须进入明确的“其他/未分类”，不得静默丢失。
- 用户手动规则优先于内置或 AI 推断；分类修改后，统计、时间线和日报必须使用统一结果。

#### FR-WR-03 时间线

- 系统必须支持按日期、应用、分类和时间范围回看工作活动。
- 时间线条目需要展示时间、应用、标题、分类和时长；工作记录截图或 OCR 开启时，再展示对应截图和 OCR 内容。
- 工作记录截图开启时，用户需要能够打开截图详情。
- 忽略和脱敏规则必须在时间线、统计、日报和远程资源中一致生效。

#### FR-WR-04 日报和阶段总结

- 系统必须能够基于指定日期的活动、分类、OCR 和时长生成结构化日报和小时/阶段总结。
- 日报的数据范围必须与对应日期时间线一致，不得混入其他日期数据。
- 配置模型后可生成 AI 增强分析；模型不可用时，仍需要提供基础结构化结果，并标明回退状态。
- 日报需要支持回看、编辑、复制和导出。
- 日报不得自动发送到微信或其他对外渠道。

#### FR-WR-05 长期整屏截图与 OCR 归档

- 工作记录链路必须支持按可配置周期长期归档当前工作屏幕截图及 OCR；长期整屏归档不设独立授权项，归档采集由“工作记录截图”和“工作记录 OCR”分别控制。
- 用户需要能够选择归档前台窗口所在屏幕或全部显示器。
- 每份归档需要关联时间、前台应用、标题、分类和活动时长；工作记录 OCR 开启时再关联 OCR 内容。
- 对非微信工作记录，用户需要能够配置截图周期、保留期限和存储上限，并可删除指定日期、应用或时间范围的记录。
- 微信提词器链路的窗口截图与 OCR 结果，其落盘义务、五年固定保留、用户提前删除和到期自动删除均按 FR-WX-07 执行。整屏归档帧中出现的微信画面属于工作记录数据，由“工作记录截图”和“工作记录 OCR”授权管辖，适用前款可配置的保留期限和存储上限，不执行五年固定保留。
- 非微信记录达到保留期或存储上限时，必须根据明确可见的规则清理最旧内容。
- 微信截图与 OCR 的五年保留规则不受普通工作记录存储上限覆盖；如果剩余空间不足，系统必须按 FR-WX-07 暂停新的微信提词处理和记录写入并提示用户处理，不得自动删除仍在五年保留期内且未被用户主动删除的微信记录。
- 微信回复模型不得自动获得其他应用的整屏归档。

#### FR-WR-06 远程上传与补传

- 系统必须提供可由用户开启或关闭的远程上传能力。
- 远程存储关闭时，不得产生上传请求。
- 远程存储开启、配置有效且用户已选择上传数据范围时，范围内的新截图及关联记录必须进入上传队列，并保存可用的远程地址和状态。
- 网络中断或服务端失败时，本地记录不得丢失；恢复后需要能够补传。
- 上传成功、失败、待补传和远程资源地址必须对用户可见。
- 微信截图与 OCR 的五年固定保留期只适用于本地落盘数据；远端副本按照用户配置的远程保留规则处理，用户主动删除本地、远端或两者时必须按所选范围执行。
- 远程存储协议和供应商未锁定；S3 和 WebDAV 仅是参考项目中的示例。

#### FR-WR-07 键鼠活动监控

- 键鼠活动监控不属于授权项，无需用户单独授权或开启；系统直接被动监测按键、鼠标移动和鼠标点击的活动状态，用于活跃/空闲判定、工作节奏统计和桌宠状态反馈。
- 工作记录的活跃/空闲判定必须使用上述键鼠活动信号；查询系统最后输入时间等只读系统调用属于本条的合法实现手段，不构成需要授权的监控行为。
- 达到用户配置的空闲阈值后，系统必须停止累计有效工作时长，空闲时间不得计为工作时长。
- 键鼠监控只记录活动状态和节奏，不持久化完整按键序列，不记录用户实际输入的文本、密码或聊天内容。
- 键鼠活动监控是只读信号，不等同于通用电脑控制，不得用它向微信产生输入。
- 用户暂停记录后，相关活动统计必须停止或进入明确暂停状态。

#### FR-WR-08 隐私规则和数据控制

- 用户可以随时暂停和恢复记录。
- 需要支持按应用设置“正常记录”、“脱敏”和“忽略”规则。
- “忽略”应用不产生截图、OCR 或活动内容；“脱敏”应用只保留允许的统计信息，不保留截图和 OCR。
- 记录权限优先级依次为：全局或功能关闭、应用“忽略”、应用“脱敏”、应用“正常记录”。微信截图或微信 OCR 已授权，不得覆盖“忽略”或“脱敏”规则；五年保留只适用于按最终生效规则成功落盘的数据。
- 将微信应用设为“忽略”或“脱敏”时，即使“微信截图”和“微信 OCR”已授权并开启，微信提词器的截图、OCR 和回复模型调用也必须暂停并显示原因；这是按应用隐私规则对微信提词器的有意覆盖，恢复“正常记录”后可以恢复。按应用的“忽略”和“脱敏”规则属于全局隐私设置，其效力不依赖“工作记录”开关本身是否开启。
- 需要提供敏感文本过滤、截图开关、OCR 开关、保留策略和数据删除能力。
- 本地与远程数据的保留状态、上传状态和删除结果必须对用户可见。
- 工作记录功能不得放宽微信保护域的限制。

### 4.3 Live2D 桌宠与多模态助手

#### FR-PET-01 桌宠常驻和窗口行为

- Live2D 角色需要常驻 Windows 桌面，普通应用切换时不退出。
- 需要支持透明背景、全局置顶、无普通窗口边框、拖动定位和鼠标穿透。
- 鼠标穿透可以开启和关闭；穿透开启时，鼠标事件落到下层应用，同时必须存在可恢复桌宠交互的明确入口。
- 用户需要能够显示、隐藏、启用、停用和调整桌宠位置。

#### FR-PET-02 Live2D 角色、动作和表情

- 需要支持加载和切换 Live2D 角色模型、头像、待机动作、交互动作和表情映射。
- 模型回复中的状态或情绪可以触发对应角色表情和动作。
- 缺少表情映射、动作文件或模型资源时，不得导致桌宠窗口退出或角色消失。

#### FR-PET-03 气泡和文字展示

- 角色旁边需要显示模型回复、思考状态、主动建议、提醒和错误信息。
- 需要支持长文本阅读、关闭、必要时的持续显示，以及可选的流式更新。
- 关闭 TTS 时，文字气泡仍必须独立正常工作。

#### FR-PET-04 ASR 语音输入

- 用户启用麦克风后，系统需要能够将语音转换为文本命令或对话输入。
- 录音期间必须有持续可见的状态，用户可以随时停止、静音或禁用。
- ASR 引擎可以是本地或远程服务，具体提供商未锁定。
- 语音中的“发送微信”命令不得触发微信输入或发送。

#### FR-PET-05 TTS 语音合成

- 系统需要能够朗读模型回复、微信建议和主动提醒，并驱动角色说话状态。
- 需要提供停止、静音和音量控制。
- TTS 可以关闭；关闭后不得影响文字回复和气泡。
- TTS 引擎可以是本地或远程服务，具体提供商未锁定。

#### FR-PET-06 摄像头与屏幕感知

- 用户主动开启后，助手可以理解摄像头画面、屏幕录制或截图内容。
- 摄像头、屏幕感知或录制期间必须有持续可见的状态提示。
- 关闭对应开关后，必须停止新的采集和分析。
- 调用远程多模态模型时，用户需要知道哪些图像或文本会被发送到外部服务。

#### FR-PET-07 整屏工作分析和主动建议

- 助手需要能够根据当前/近期的屏幕内容、应用、时长、OCR 和键鼠节奏生成工作建议。
- 主动建议可以通过气泡、角色表情和 TTS 展示。
- 用户需要能够关闭主动建议、暂停分析、设置频率或免打扰时段。
- 主动建议只能展示或朗读，不得未经用户确认自动执行电脑操作。
- 未开启本功能时，长期整屏归档不得自动发送给模型。

#### FR-PET-08 直播模式（后续版本，非初版）

- 系统需要支持连接用户配置的直播平台或直播间，接收实时聊天/弹幕事件。
- Live2D 角色需要能够使用文字、表情和语音对直播事件进行响应。
- 直播状态、连接状态、正在使用的角色和模型必须对用户可见；停止直播模式后必须终止连接。
- 微信聊天画面、OCR 文本和回复建议不得未经用户明确操作自动进入直播输出。
- 具体直播平台未锁定；B 站直播只是参考项目已有实现之一。

#### FR-PET-09 MCP 工具（后续版本，非初版）

- 系统需要支持连接、启用、禁用和管理用户允许的 MCP 服务。
- 模型需要能够发现可用工具、提出调用并获得结果。
- 用户必须能够看到已启用的 MCP 服务、工具调用的运行/成功/失败状态和必要的确认。
- MCP 工具不得对识别为微信的目标窗口执行操作，不得绕过本文档中的窗口级权限、隐私和确认规则。

#### FR-PET-10 通用电脑控制（后续版本，非初版）

- 在微信之外，系统需要能够根据用户明确指令执行鼠标、键盘、窗口、应用和文件操作。
- 执行前需要确认当前目标窗口和输入焦点不属于微信。
- 高风险、不可逆、删除、对外发布、支付、授权或凭据相关操作需要用户明确确认。
- 必须提供可见的当前任务状态、立即停止入口和失败后可理解的反馈。
- 通用电脑控制不得由主动建议静默触发，也不得在直播模式中因弹幕指令自动执行。

#### FR-PET-11 角色和模型配置

- 初版用户需要能够配置 Live2D 角色、角色名、人设、LLM、ASR、TTS 和声音；后续版本增加直播、MCP 和通用电脑控制开关。
- 切换角色、模型或工具配置不得改变微信保护域和用户确认规则。
- 本地引擎和远程 API 都可以成为配置选项，本文档不锁定具体提供商。

### 4.4 初版本地 OCR 能力

#### FR-OCR-01 模块职责

- OCR 模块只负责识别上游已经获取的图片，不负责查找微信窗口、选择聊天区域、定时截图或控制微信。
- 初版必须提供在本机离线运行的本地 OCR 识图能力；该路径不得把识别图片或结果发送到外部网络。
- 初版微信提词和工作记录中的图片文字识别必须使用本地 OCR，不使用远程 OCR API 或多模态 LLM 图像识别。
- RapidOCR 和 Umi-OCR 中的本地 RapidOCR 插件属于候选参考实现，不构成对具体 OCR 引擎的强制锁定。

#### FR-OCR-02 输入和输出

OCR 至少需要支持以下图片输入形式：

- 本地图片路径。
- 图片字节。
- 不含 `data:image/...;base64,` 前缀的 Base64 字符串。

上游截图模块至少需要可以直接传入内存图片数据，不能要求用户手工保存或选择文件。

每个结构化文本块至少需要包含：

- `text`：识别文字。
- `score`：0～1 的识别置信度。
- `box`：基于输入图片坐标系的文本框四角坐标。
- `end`：本文本块与下一块之间的连接符，可以是空字符串、空格或换行。

OCR 返回需要明确区分：

- 识别成功且存在文字。
- 识别成功但图片中无文字。
- 识别失败，并提供可理解的错误原因。

返回中需要包含本次识别耗时；OCR 任务时间戳不得被解释为微信消息时间。

#### FR-OCR-03 文本排序和段落拼接

- OCR 需要保留原始文本块坐标和置信度。
- 需要支持按几何阅读顺序对文本块进行排序，并添加段落连接符。
- 按返回顺序拼接每个文本块的 `text + end` 后，需要能得到对应的完整纯文本。
- OCR 层的排序只表示图片中的几何阅读顺序，不表示已经判断聊天双方、消息归属、联系人、消息时间或新消息状态。

#### FR-OCR-04 可配置语义

- 至少满足简体中文聊天文字识别。
- 保留切换识别语言、文本方向纠正、最大图片边长、排版解析方式、忽略区域和输出格式的配置语义。
- 具体可用参数必须以初版实际采用的本地 OCR 引擎所暴露的选项为准，不将 Umi-OCR 或 PaddleOCR 文档中的示例参数写成所有本地 OCR 引擎的固定参数。

#### FR-OCR-05 Umi-OCR 明确排除范围

不采用 Umi-OCR 的以下完整桌面软件功能：

- PySide/QML 主界面、标签页、主题和多语言界面。
- 手工框选截图、截图历史、结果编辑、图片预览和剪贴板图片识别入口。
- 批量文件队列、任务暂停/恢复和批量完成后的关机/待机。
- TXT、JSONL、Markdown、CSV 等多格式导出。
- PDF 文档识别、二维码和公式识别。
- Umi-OCR 自带的系统托盘和结果展示界面。

Umi-OCR 参考实现和初版本地 OCR 模块也不负责：

- 查找、选择或跟踪当前处于前台的已选定微信聊天窗口。
- 确定聊天区域或执行定时截图。
- 判断文字属于我方还是对方。
- 将文本块划分成微信消息或理解消息语义。
- 比较前后两帧、判断内容是否变化、检测新消息或去重。
- 调用大模型、显示气泡、复制建议或操作微信。

### 4.5 后续版本微信与工作记录多模态 LLM 图像识别

#### FR-VISION-01 本地 OCR 替换范围

- 后续版本规划使用可配置的多模态 LLM 图像识别替代初版的本地 OCR，覆盖微信聊天图片识别和工作记录图片识别链路。
- 该替换不属于初版交付或验收范围，初版必须能够完全依靠本地 OCR 工作。
- 本节只规定对 4.1 微信提词和 4.2 工作记录中本地 OCR 文字识别链路的后续替换，不影响 4.3 中初版必须完成的摄像头与屏幕感知能力。
- 多模态 LLM 图像识别首次启用时必须保持关闭，并与云模型的其他用途分别授权。
- 应用升级到支持本能力的后续版本后，在用户完成单独授权和迁移确认前，微信及工作记录两条文字识别链路继续使用本地 OCR，不得自动切换或停用。
- 用户确认迁移后，微信及工作记录两条文字识别链路同时切换到多模态 LLM；用户必须能够显式回退到本地 OCR。
- 启用前必须让用户知道将发送的图片范围、文本范围和目标服务；未获得授权时不得产生图像外发请求。
- 后续微信识别链路仍然只能向多模态 LLM 提供当前处于前台的已选定微信聊天窗口内、经过聊天区域裁剪后的授权图像，不得把其他微信窗口或窗口外桌面区域作为输入。
- 后续识别结果仍必须满足本文档规定的文本块、置信度、坐标、顺序、失败状态和聊天状态闭锁要求，或在后续版本需求基线中定义等价且可验收的结构。

## 5. 数据与状态需求

### 5.1 数据分域

以下数据必须在逻辑上分离：

- 微信已选定聊天窗口的截图和当前聊天 OCR 上下文。
- 微信窗口绑定、会话键、基线有效状态、截图版本、请求 ID、增量消息状态和回复建议。
- 工作活动、整屏截图和长期 OCR 归档。
- 日报、阶段总结和工作建议。
- 摄像头和麦克风临时上下文，以及后续版本的直播和 MCP 临时上下文。

微信回复模型不得读取其他应用的整屏长期归档。各数据类型的采集、落盘、读取和上传所需授权以下表为准；未获得对应授权的模块不得自动取得这些数据：

| 数据类型 | 采集与落盘所需授权 | 保留规则 | 其他模块读取 | 远程上传 |
| --- | --- | --- | --- | --- |
| 已选定微信窗口截图 | “微信提词”+“微信截图” | 五年固定保留 | 读取方须持自身授权：工作记录域模块（时间线、日报）须开启“工作记录”；分析模块须开启“整屏工作分析” | 须开启“远程上传”并选择上传范围 |
| 微信聊天 OCR 结果 | “微信提词”+“微信截图”+“微信 OCR” | 五年固定保留 | 同上 | 同上 |
| 工作活动记录（应用、标题、时长、分类） | “工作记录” | 按用户配置 | 属工作记录域，域内共享；整屏工作分析读取须开启“整屏工作分析” | 同上 |
| 工作记录截图与整屏归档帧（含画面中可见的微信内容） | “工作记录”+“工作记录截图” | 按用户配置的保留期限和存储上限 | 同上 | 同上 |
| 工作记录 OCR 文本 | “工作记录”+“工作记录截图”+“工作记录 OCR” | 按用户配置 | 同上 | 同上 |
| 键鼠活动状态 | 无（非授权项，系统直接采集） | 按用户配置，不持久化按键序列 | 整屏工作分析读取须开启“整屏工作分析” | 同上 |
| 日报、统计等派生数据 | 由已授权数据派生，不新增采集授权 | 随源数据规则 | — | 同上 |
| 摄像头画面、麦克风音频、屏幕感知实时画面 | “摄像头”/“麦克风”/“屏幕感知” | 临时上下文，不长期落盘 | — | 仅在“云模型”等对应外发授权后发送 |

- 整屏归档帧中拍到的微信画面属于工作记录数据，由“工作记录截图”和“工作记录 OCR”授权管辖，执行工作记录保留规则；五年固定保留只适用于微信提词器链路按“微信截图”和“微信 OCR”落盘的窗口截图与 OCR 结果。
- 应用级“忽略”和“脱敏”规则（FR-WR-08）优先于上表全部采集授权。
- 上表任何数据交给远程模型（LLM、远程 ASR/TTS、远程多模态）处理前，须另开“云模型”，并让用户知道将发送的数据范围。
- 直播、MCP 和通用电脑控制属于后续版本，不得因其他功能已获授权而自动获得上表数据。

### 5.2 记录、保留和删除

- 用户需要能够查看数据存储位置、数量、占用空间、保留期限和远程上传状态。
- 用户需要能够按日期、时间范围、应用或记录类型删除数据。
- 删除操作需要明确告知将删除本地数据、远程数据或两者。
- 已授权采集的微信截图和微信 OCR 结果必须写入本地存储，自采集时间起保留五年；用户主动删除时可以提前清除，达到五年时必须自动清除。
- 本需求不要求业务截图、OCR 结果和关联记录在本地或远程存储中采用静态数据加密，不把该类存储加密作为初版或后续版本的验收项；此决定不免除文件访问权限、日志脱敏、凭据保护以及外发前单独授权、过程可见且可关闭的要求。
- API 密钥、远程存储凭据和 MCP 凭据不得出现在普通日志、OCR 文本、截图元数据或日报中。

## 6. 参考项目范围和决策

### 6.1 Work-Review

Work-Review 是本产品的主要整体参考，需要参考的能力包括：

- Windows 前台窗口识别。
- 后台定时采集和截图。
- OCR 任务和变化去重思路。
- 工作时长统计、应用分类、时间线和日报。
- 远程上传和长期整屏归档。
- 键鼠活动、空闲和锁屏判断。
- 桌宠状态、气泡和大模型调用链路。
- 隐私规则、脱敏、忽略和数据清理。

当前本地 Work-Review 参考代码已经能够通过 Windows 系统能力取得当前前台窗口、进程、标题和窗口边界。当已选定微信聊天窗口处于前台时，该能力可以承担“确认并获取当前处于前台的已选定微信聊天窗口”的参考职责。

该参考能力不等同于已经实现用户选择、窗口绑定、绑定失效或重新选择流程，这些仍须由本项目实现。

但必须区分：Work-Review 当前的截图逻辑主要截取前台窗口所在显示器，不等同于已完成“只截取微信窗口/聊天区域”。Work-Review 现有 OCR 和去重也不等同于已完成微信聊天双方判断或新消息识别。

Work-Review 的 Rust、Tauri、Svelte、SQLite、模型提供商、远程协议、默认截图周期、图像相似度阈值和保留天数都只是参考实现，不是已确定的产品需求。

### 6.2 deepseek_wechat_bot

本需求基线不再将 `deepseek_wechat_bot` 纳入实现参考范围，原因是 Work-Review 已具备读取 Windows 当前前台窗口及边界的参考能力。

移除该参考不代表微信聊天解析需求被移除。微信窗口内的聊天区域确定、气泡归属、消息结构和新消息判断仍然是本项目自身需求，不由 Work-Review 或 Umi-OCR 自动提供。

### 6.3 Open-LLM-VTuber

需要参考的范围包括：

- Live2D 角色、资源、动作和表情映射。
- 透明背景桌宠、全局置顶、拖动和鼠标穿透。
- 模型回复文字、角色信息和表情动作向前端的传递。
- 初版所需的 ASR、TTS、摄像头/屏幕感知和主动建议。
- 后续版本所需的直播交互、MCP 工具和通用电脑控制的产品能力与交互形态。

当前本地参考目录中的 `frontend/` 为空，实际前端是外部子模块；因此不能假定本地拷贝已经包含透明置顶窗口、鼠标穿透和回复气泡的完整前端源码。

当前 Open-LLM-VTuber 参考也不能被表述为已完成微信 OCR、建议复制按钮或通用电脑控制的全部执行能力。其中微信 OCR 和建议复制属于初版必需能力，通用电脑控制属于后续版本能力；均不能假定可以从当前本地代码中直接获得。

### 6.4 Umi-OCR

Umi-OCR 的参考范围仅限：

- 可供初版本地 OCR 选型参考的 RapidOCR 插件和模型。
- 图片路径、字节和 Base64 识别。
- 包含文字、置信度、坐标和行尾结构的结果。
- 文本块几何排序、忽略区域和段落拼接。
- 本机进程间调用的接口语义。

其他功能和不属于 OCR 层的职责，以 FR-OCR-05 为准。初版具体采用哪个本地 OCR 引擎以实现阶段决策为准；后续版本按 FR-VISION-01 改用多模态 LLM 图像识别。

## 7. 非功能需求

### 7.1 平台和技术中立

- 初版产品必须在 Windows 11 上运行，并适配 Windows 微信 4.0.x 系列客户端。
- 初版不要求适配 Windows 10、企业微信或 Windows 微信 4.0.x 系列之外的客户端版本。
- 初版固定使用本地 OCR 能力，但不锁定具体本地 OCR 引擎；后续版本规划改用多模态 LLM 图像识别。
- 编程语言、桌面 UI 框架、本地数据库、截图实现、LLM/ASR/TTS 供应商、后续直播平台和远程存储协议都未锁定。
- 参考项目的技术选择和默认参数不能自动转化为本项目约束。

### 7.2 性能和资源

- 截图、OCR、模型调用和归档周期需要可配置。
- 未变化的微信聊天不得重复调用回复模型，避免重复提词和 API 浪费。
- 非微信长期整屏归档必须受保留期和存储上限约束，不得无限制增长；微信截图与 OCR 按五年规则保留，空间不足时按 FR-WR-05 停止新增并提示用户处理。
- 本地 OCR、LLM、远程上传、ASR 或 TTS 任一能力失败时，桌宠和基础本地记录不得整体崩溃。
- 本需求基线不自行设定固定响应时间、OCR 准确率、截图间隔或图像相似度阈值，这些需要后续根据测试样本确定。

### 7.3 隐私和安全

- 屏幕、摄像头、麦克风、直播、远程上传和云模型的开启状态必须持续可见。
- 任何将截图、摄像头画面、OCR 文本或聊天内容发送到外部服务的行为都必须可配置、可见且可关闭。
- 首次启动时 3.3 授权项总表所列全部初版授权项必须默认关闭并分别授权；后续版本的直播、MCP 和电脑控制能力在提供时同样默认关闭并分别授权。
- 微信截图和 OCR 可以在获得对应授权后落盘、读取和上传，保留规则以 5.2 为准。
- 本需求不要求业务截图、OCR 结果和关联记录采用本地或远程静态存储加密；文件访问权限、日志脱敏、凭据保护以及外发前单独授权、过程可见且可关闭的要求仍然有效。
- 不得在日志中记录 API 密钥、存储凭据、MCP 凭据或用户实际键盘输入。
- 系统必须提供明确的暂停、禁用、立即停止和数据删除入口。
- 微信保护域中的窗口级禁止规则不得被其他模式、插件、模型或工具覆盖。

### 7.4 可用性

- 桌宠和气泡不得阻挡微信或其他应用的主要操作区域，用户可以移动、隐藏或开启鼠标穿透。
- 采集、识别、模型生成、复制、上传、直播和工具调用均需要有可理解的状态和失败反馈。
- 初版用户可以分别关闭 3.3 授权项总表所列的全部 13 项能力；后续版本还必须能够分别关闭直播、MCP 和电脑控制。

## 8. 关键验收场景

### AC-01 微信回复完整链路

1. 用户在 Windows 11 的 Windows 微信 4.0.x 系列客户端中，手动选择并绑定一个顶层聊天窗口。
2. 该已选定微信聊天窗口处于前台，首次采样只建立当前聊天基线，不生成建议。
3. 基线建立后，对方发来一条新消息。
4. 系统完成“已选定窗口判定 → 截图 → 本地 OCR → 新对方消息判断 → 模型建议 → 桌宠气泡/可选 TTS”。
5. 用户点击复制后，剪贴板获得建议正文。
6. 全程微信没有收到任何自动输入、鼠标点击或发送操作。

### AC-02 重复抑制和聊天隔离

- 连续多次截取未变化的聊天画面，只允许生成一次建议。
- 只有我方新消息时，不生成新建议。
- 切换聊天对象后，不复用上一个聊天的增量状态和建议。
- 首次绑定、重新进入聊天或重新建立窗口绑定时，只建立基线，当前可见历史消息不触发建议。
- 向上滚动后出现历史消息时，不触发建议。
- 群聊对象、具体发送者或消息归属无法确认时，不调用模型；能够确认时，按群聊会话隔离状态，并记录已可靠识别的具体发送者。
- 在同一窗口绑定内，从聊天 A 切换到显示名称与 A 相同的聊天 B 时，必须产生新会话键并清空 A 的增量状态和建议；B 的首次采样只建立基线，在 A 中已出现过的内容不得作为 B 的新消息触发建议。
- 当前聊天对象、发送方或新旧状态无法确认时，停止触发、清空或隔离旧状态，并显示无法确认的状态。
- 模型生成期间发生聊天切换、窗口失效、截图版本更新或新消息更新时，旧请求必须取消或丢弃，其结果不得显示、朗读或复制。

### AC-03 微信保护域拦截

- 用户语音说“把这句发出去”时，助手可以生成或修改建议，但必须拒绝代发。
- 以下两项属于后续版本验收，不影响初版通过：
    - MCP 或主动模型试图调用“点击微信发送”时，执行层必须按目标窗口和输入焦点在输入事件发生前拒绝并显示原因。
    - 通用电脑控制可以在用户授权后向记事本输入文字；将相同动作的目标换成微信时，必须按窗口级保护规则阻断。

### AC-04 初版本地 OCR

- 在断开外网的 Windows 11 环境中，给定一张有效的中文聊天区域图片，初版采用的本地 OCR 仍能返回结果。
- 在断开外网的 Windows 11 环境中，给定一张非微信工作屏幕图片，工作记录链路也必须使用本地 OCR 返回结果，不得调用远程 OCR API 或多模态 LLM。
- 有文字、无文字、损坏图片/非法输入和引擎不可用必须返回可区分的状态。
- 结构化模式中的有效文本块都包含 `text`、`score`、`box` 和 `end`，所有坐标都在输入图片范围内。
- 按 `text + end` 顺序拼接后与纯文本模式结果一致。
- OCR 调用不弹出独立 OCR 桌面主窗口，不要求手工框选，不创建批处理任务，不修改剪贴板，不操作微信，也不向外部网络发送识别图片或结果。

### AC-05 工作时长、分类、时间线和日报

- 受控执行“应用 A → 应用 B → 空闲/锁屏”后，记录顺序正确，时长误差不超过一个采集周期。
- 用户手动修改分类后，统计、时间线和日报使用一致的新分类。
- 指定日期的时间线可以回看截图和 OCR，日报数据与该日时间线一致。
- 只开启工作记录而关闭工作记录截图和工作记录 OCR 时，仍能生成仅包含时间、应用、标题、分类和时长的合法活动记录、时间线和基础日报。
- 只开启工作记录、不开启其他能力时，系统无需额外授权即可进行活跃/空闲判定；达到空闲阈值后停止累计有效工作时长，空闲时间不得计入。
- 暂停、锁屏、忽略应用和脱敏应用按各自规则处理，不从时间线、日报或远程资源泄漏被过滤内容。

### AC-06 长期归档和远程上传

- 开启工作记录和工作记录截图后，长期整屏归档随工作记录链路运行，新活动记录能关联整屏截图；同时开启工作记录 OCR 后再关联 OCR 结果。
- 非微信记录达到配置保留期或存储上限后，按明确规则清理最旧数据。
- 开启微信截图后，微信提词器链路的窗口截图必须落盘；另行开启微信 OCR 后，其 OCR 结果也必须落盘。两类记录均标明五年到期时间，用户可提前删除，达到五年时自动删除。
- 整屏归档帧中拍到微信画面时，该帧属于工作记录数据，按用户配置的工作记录保留期限和存储上限清理，不执行五年固定保留。
- 存储空间不足或微信记录落盘失败时，新的微信截图、OCR、回复模型调用和上传暂停，已有记录仍可读取；清理出足够空间后可以恢复。
- 关闭远程存储时无上传请求；开启后可识别上传成功、失败和待补传状态。
- 网络中断后本地记录不丢失，网络恢复后可以补传。
- 微信五年固定保留期只适用于本地记录；远端副本按用户配置的远程保留规则处理，本地和远端删除范围能够分别选择并显示结果。

### AC-07 桌宠多模态能力

- Live2D 角色可常驻、置顶、拖动，并切换鼠标穿透。
- Live2D 角色可以切换模型、动作和表情映射；切换角色或模型配置后，桌宠和气泡仍可继续工作。
- 麦克风授权并启用后，ASR 可以把语音转换为文本命令或对话输入；关闭麦克风后停止新的语音采集。
- 关闭 TTS 后文字气泡仍正常；开启 TTS 后能够实际朗读模型回复并驱动角色说话状态，同时可以停止和静音。
- 摄像头或屏幕感知分别授权并启用后，系统可以理解对应输入并产生可见的文字结果；关闭后停止新的采集和分析。
- 整屏工作分析和主动建议可以分别启用、暂停和关闭，关闭后不再产生新的分析或主动建议。

### AC-08 默认关闭与分别授权

- 全新安装或清除配置后首次启动，3.3 授权项总表所列全部初版授权项均为关闭状态。
- 3.3 授权项总表所列全部初版授权项必须能够分别授权和启用。
- 开启任一能力不会自动开启其他能力；未授权的能力不产生新的采集、识别、外部请求或上传。
- 微信应用设为“忽略”时，即使微信截图或微信 OCR 已开启，也不产生截图、OCR 或活动内容，微信提词器的截图、OCR 和回复模型调用必须暂停并显示原因；设为“脱敏”时只保留允许的统计信息，不落盘截图或 OCR，微信提词器同样必须暂停并显示原因。
- 后续版本加入直播、MCP 和通用电脑控制时，这三项也必须默认关闭并分别授权。

### AC-09 后续版本能力（不纳入初版验收）

- 直播模式可以接收实时消息并输出文字、表情和语音，停止后终止连接。
- MCP 服务可以启用/禁用，工具调用有运行和结果状态。
- 通用电脑控制可以在微信之外按用户指令执行，并能立即停止。
- 升级后未授权多模态 LLM 图像识别时，微信和工作记录继续使用本地 OCR，不产生图像外发请求。
- 用户确认迁移后，微信聊天和工作记录两条 OCR 链路同时切换到多模态 LLM；显式回退后，两条链路恢复本地 OCR。
- 两条 OCR 链路迁移或回退时，FR-PET-06 的摄像头与屏幕感知配置、授权和输入处理不受影响。
- 多模态 LLM 图像识别替代本地 OCR 后，继续满足后续版本冻结的识别结果和状态闭锁要求。

## 9. 明确不锁定的事项

初版和后续版本的功能划分已经在 2.1 中锁定。以下内容仍留待对应实现阶段作产品或技术决策；在进入相关模块开发前，需要形成具体方案，但不得改变本文档已经明确的版本范围和窗口级微信保护边界：

- 编程语言和整体桌面技术栈。
- 具体本地数据库、数据模型和存储目录。
- 微信窗口截图和聊天区域确定的具体实现方式。
- 微信气泡归属、消息分组和增量标识的具体算法。
- 截图周期、OCR 响应时间、识别准确率、图像相似度和空闲阈值。
- LLM、初版本地 OCR 引擎、ASR、TTS 和后续多模态模型供应商。
- 首个支持的直播平台。
- 远程存储供应商和协议。
- 电脑控制中不同风险级别的最终确认策略。
- 初版范围内各功能模块的内部开发顺序。

本需求明确不把微信间接操作识别或焦点切换竞态防护纳入验收；微信保护按本文档规定的目标窗口、当前前台窗口和输入焦点执行窗口级判断。

## 10. 需求结论

本产品不再只是单一的微信回复提词器，而是一个运行在 Windows 11、以 Live2D 桌宠为交互载体的 AI 桌面助手。初版包含微信只读回复、个人工作记录、整屏归档、工作分析、语音、摄像头与屏幕视觉能力；直播、MCP、通用电脑控制以及替代本地 OCR 的多模态 LLM 图像识别属于后续版本。

其中，微信回复始终是不可被其他能力放宽的窗口级特殊保护域：系统只能处理当前处于前台的已选定 Windows 微信 4.0.x 聊天窗口，可以在分别授权后保存、读取和上传微信截图及 OCR；在本文档验收覆盖的窗口级交互范围内，系统不得代替用户粘贴或发送。微信截图和 OCR 结果落盘后保留五年，用户可以提前删除；本需求不要求业务截图、OCR 结果和关联记录采用静态存储加密。
