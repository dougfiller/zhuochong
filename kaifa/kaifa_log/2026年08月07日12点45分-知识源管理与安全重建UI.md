# 变更记录：知识源管理与安全重建 UI（202608071245）

## 0. 变更元信息

| 字段 | 内容 |
| --- | --- |
| 变更时间 | 2026-08-07 12:45 |
| 变更类型 | 新增本地知识源管理界面与 Store 门面 |
| 关联方案 | `2026年08月07日12点35分-知识源管理与安全重建UI.md` |
| 范围 | 步骤 19 / 本 LOOF run |

## 1. 改动目标

- 允许用户显式选择一次导出目录并经既有 `KnowledgeStore` 导入。
- 以 Store 的脱敏 DTO 显示来源、导入状态和计数；不显示或保存路径、正文或原始导出标识。
- 提供退役、拒绝和候选 SQLite/manifest 重建入口；拒绝会让 active read fail-closed。

## 2. 详细改动

| 路径 | 改动 |
| --- | --- |
| `desktop/src-tauri/src/knowledge/store.rs` | 增加来源清单、单维护 lease、退役/拒绝、维护 read gate、候选库导入、校验、同目录 rename 交换与红acted manifest。 |
| `desktop/src-tauri/src/knowledge/commands.rs`、`main.rs` | 注册来源清单、导入、维护状态、退役、拒绝和重建 command；所有失败使用稳定 `KB_NOT_READY`。 |
| `desktop/src/routes/settings/components/SettingsKnowledge.svelte` | 增加显式目录选择、确认框、禁用态、轮询和脱敏清单；不再从旧 config 来源数组读取事实。 |
| `desktop/src/lib/i18n/locales/*` | 增加四种语言的本区文案。 |
| `kaifa/kaifa_test/*` | 增加静态门禁、设置页断言和测试说明。 |

## 3. 安全边界

- 目录路径仅在一次 command 调用栈中使用；不进入配置、DTO、Toast、日志或 manifest。
- 导入继续复用既有 archive read guard 和 Store，未新增 SQLite 所有者、扫描器、网络或微信自动化。
- 重建失败或候选校验失败不会 copy-overwrite 主库；读取在维护期间 fail-closed。

## 4. 验收结果

| 检查 | 实际结果 |
| --- | --- |
| `python3 -B kaifa/kaifa_test/verify_knowledge_source_management.py` | `KNOWLEDGE_SOURCE_MANAGEMENT_GATE: pass` |
| `node --test desktop/src/routes/settings/SettingsWechatKnowledge.test.js` | 1/1 通过 |
| `cargo check --manifest-path desktop/src-tauri/Cargo.toml --no-default-features --features 'wechat-contract-check,wechat-m2'` | 通过；仅有既有 dead-code 警告 |
| `git diff --check -- <本步骤路径>` | 通过 |

## 5. 边界与回滚

- 未运行真实导出或 Windows UAT；其结果不由本阶段的 macOS 编译替代。
- 回滚应撤销本步骤提交；不得删除 `wechat_knowledge/` 中的候选或备份派生数据。
